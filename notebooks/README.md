Notebook guidelines

- Put narrative notebooks in notebooks/ with numeric prefixes `01-` for order.
- Use `nbconvert` or the Jupyter UI to export HTML for reports/ when ready.
- Keep exploratory work in separate notebooks from the final narrative.

Suggested first notebook: `01-exploratory.ipynb` — load data from data/, show EDA, save key visuals to visuals/.